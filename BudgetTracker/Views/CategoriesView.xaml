<UserControl x:Class="BudgetTracker.Views.CategoriesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <TextBlock Text="Categories" FontSize="28" FontWeight="Bold" Margin="0 0 0 16"/>
        <StackPanel Orientation="Horizontal" Margin="0 0 0 8">
            <TextBox Width="220" Text="{Binding Name}" Margin="0 0 8 0"/>
            <ComboBox Width="120" SelectedItem="{Binding Type}" ItemsSource="{Binding TransactionTypes}" Margin="0 0 8 0"/>
            <Button Content="Add" Command="{Binding AddCategoryCommand}"/>
        </StackPanel>
        <DataGrid ItemsSource="{Binding Categories}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}"/>
                <DataGridTemplateColumn Header="Action">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Delete"
                                    Command="{Binding DataContext.DeleteCategoryCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
</UserControl>
