<UserControl x:Class="BudgetTracker.Views.TransactionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Transactions" FontSize="28" FontWeight="Bold" Margin="0 0 0 16"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0 0 0 10">
            <DatePicker SelectedDate="{Binding Date}"/>
            <ComboBox Width="100" Margin="8 0" SelectedItem="{Binding Type}" ItemsSource="{Binding TransactionTypes}"/>
            <ComboBox Width="140" Margin="8 0" ItemsSource="{Binding Accounts}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedAccount}"/>
            <ComboBox Width="140" Margin="8 0" ItemsSource="{Binding Categories}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedCategory}"/>
            <TextBox Width="100" Margin="8 0" Text="{Binding Amount}"/>
            <TextBox Width="200" Margin="8 0" Text="{Binding Description}"/>
            <TextBox Width="100" Margin="8 0" Text="{Binding SelectedMonth}" ToolTip="YYYY-MM"/>
            <Button Content="Add" Command="{Binding AddTransactionCommand}" Margin="8 0"/>
            <Button Content="Export CSV" Command="{Binding ExportCsvCommand}" Margin="8 0"/>
            <Button Content="Import CSV" Command="{Binding ImportCsvCommand}" Margin="8 0"/>
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Transactions}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat=d}"/>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}"/>
                <DataGridTextColumn Header="Account" Binding="{Binding Account.Name}"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category.Name}"/>
                <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat=N2}"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}"/>
                <DataGridTemplateColumn Header="Action">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Delete"
                                    Command="{Binding DataContext.DeleteTransactionCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
